# Домашнє завдання 07. Регуляризація, дерева, Random Forest, XGBoost і класифікація

**Дата заняття:** 16.06.2026 (вт)  
**Модуль:** Модуль 3. Алгоритми навчання з учителем

## Мета

Відпрацювати регуляризацію, дерева рішень, ансамблі та базові метрики класифікації на зрозумілих supervised learning задачах.

## Завдання

Створити notebook `homework_supervised_models.ipynb`.

### Регуляризація на California Housing

#### Завдання

1. Використовуйте датасет `California Housing`.
2. Візьміть ознаки `MedInc`, `HouseAge`, `AveRooms`, `AveBedrms`.
3. Побудуйте дві моделі: `LinearRegression`; `Lasso(alpha=0.1)`.
4. Розділіть дані на `train/test` (`80/20`, `random_state=42`).
5. Для кожної моделі обчисліть R² на тестовій вибірці.
6. Порівняйте коефіцієнти моделей: скільки з них стали рівними 0 у `Lasso`.

### Класифікація цін на житло

#### Завдання

1. Використовуйте `California Housing`.
2. Перетворіть цільову змінну на бінарну: клас 1, якщо ціна вища за медіану, клас 0, якщо ціна нижча або дорівнює медіані.
3. Для цього обчисліть `np.median(y)` і створіть `y_class = (y > median_price).astype(int)`.
4. Розділіть дані на `train/test` (`80/20`, `random_state=42`).
5. Навчіть модель `LogisticRegression(max_iter=1000)`.
6. Побудуйте `confusion matrix` і виведіть Accuracy, Precision, Recall, F1.
7. Напишіть короткий висновок: що показують метрики і яку помилку модель робить частіше.

## Що здати

- Файл `homework_supervised_models.ipynb`.
